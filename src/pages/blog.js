import React from "react";
import Layout from "./../components/Layout";

const BlogPage = () => {
    return (
        <Layout>
            <div>Blog page list :</div>
        </Layout>
    );
};

export default BlogPage;
